#include <iostream>

using namespace std;

double potega(double x, int d)
{
    int b=x;
    for(int i=1; i<abs(d); i++){
        x=x*b;
    }
    if (d>0)
    cout<<x;
    if(d==0)
    cout<<"1";
    if(d<0)
    cout<<"1/"<<x;
}

int main()
{
    potega(5, -3);
}

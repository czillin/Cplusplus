#include <iostream>

using namespace std;

void rozklad(int n)
{
    for(int i=2; i<=n; i++)
    {
        if(n%i==0)
        {
            cout<<i<<" ";
            return rozklad(n/i);
        }
    }
}

int main()
{
    rozklad(1200);
}

#include <iostream>

using namespace std;

double e(double x)
{
    int a=x,b=1, s=1;
    for(int i=1; i<5; i++)//porawić i>0
    {
        x=a*x;
        b=b*i;
        s=s+x/b;
    }
    cout<<x<<" "<<b<<" "<<s;

}

int main()
{
    e(1);
}
